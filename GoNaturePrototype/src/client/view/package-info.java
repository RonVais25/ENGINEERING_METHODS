/**
 * Client JavaFX controllers and view helpers — one controller per FXML screen
 * (login, dashboard, reservations, gate, reports, approval queue, notifications,
 * registration, …) plus shared UI utilities such as {@code BaseController} and
 * {@code Widgets}. Controllers bind user actions to the {@code NetworkService}
 * and render responses; they hold no business logic.
 */
package client.view;
