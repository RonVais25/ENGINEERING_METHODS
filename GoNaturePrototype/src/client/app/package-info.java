/**
 * Client-side application scaffolding: screen navigation and the in-memory
 * session. {@link client.app.Navigator} owns the single window and swaps the
 * active FXML view, while {@link client.app.Session} holds the logged-in user
 * and per-login state shared across controllers.
 */
package client.app;
