/**
 * Server application entry point: the {@code main} that launches the GoNature
 * server process, loads configuration, and starts the network listener and
 * scheduled jobs.
 */
package server.app;
