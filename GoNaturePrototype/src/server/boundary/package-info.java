/**
 * Server JavaFX boundary: the operator GUI that starts/stops the server, shows
 * connected clients, and streams the activity log. It implements
 * {@link server.net.ServerListener} so network events surface on screen.
 */
package server.boundary;
