package server.control.entrance;

public class WalkInController {
    public int registerWalkIn(int parkId, int visitorsCount) {
        // Planned: check capacity and insert visit with entry_type WALK_IN.
        return -1;
    }
}
